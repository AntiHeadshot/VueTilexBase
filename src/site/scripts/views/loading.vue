<template>
  <div>
    <p>Loading ...</p>
  </div>
</template>
<script>
import shared from '../shared';

export default {
  created: () => {
    // if(shared.isLoggedin)
    //   router.push('createAccess');
    // else
    //   router.push('login');
  },
};
</script>